# Pinned for reproducible Railway/HF deploys and sklearn pickle compatibility.
#
# Python 3.13: pandas 2.2.2 has no cp313 wheels on Linux (pip falls back to sdist → meson
# build failures). Use pandas>=2.2.3 and numpy 2.x so pip installs wheels only.
#
# model_pipeline.pkl was saved with scikit-learn 1.6.1 — pin the same minor line (<1.7 avoids
# ColumnTransformer pickle breakage on sklearn 1.7+).
streamlit==1.37.1
pandas==2.2.3
numpy==2.2.6
scikit-learn==1.6.1
lightgbm==4.5.0
joblib==1.4.2
matplotlib>=3.8.0
plotly>=5.18.0
altair>=5.0.0,<6
